import React from "react";
import Scaffold from "../../components/Scaffold/Scaffold";

const Home = ({setLocale}) => {
    return (
        <Scaffold setLocale={setLocale}/>
    )
}

export default Home;
